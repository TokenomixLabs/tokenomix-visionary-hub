[build.environment]
  NODE_VERSION = "18.19.0"
  NPM_FLAGS = "--legacy-peer-deps"

[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: deep links must resolve to index.html instead of a raw 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
